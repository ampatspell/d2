{
  "name": "@d2/firebase",
  "private": true,
  "scripts": {
    "functions:build": "(cd functions && npm run build)",
    "firebase:deploy": "firebase deploy",
    "deploy": "npm run functions:build && npm run firebase:deploy"
  }
}
